{
  "name": "metrack",
  "version": "2.8.3",
  "private": true,
  "type": "module",
  "description": "Offline-fähiger Fitness-Tracker für GitHub Pages",
  "scripts": {
    "dev": "node scripts/preview-server.mjs",
    "check": "node scripts/check-syntax.mjs && node scripts/check-static.mjs",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
